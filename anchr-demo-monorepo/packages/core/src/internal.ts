/**
 * Internal implementation — NOT part of public surface.
 * Do not import from other packages; boundary violation if you do.
 */
export function internalHelper(): string {
  return "internal";
}
