# Run metadata

Do not summarize. Store raw.

Exact CLI command used:

ANCHR_REPORT_PATH=<path> BASE_SHA=<base> HEAD_SHA=<head> npx tsx scripts/anchr-structural-audit.ts

(From repo root, run from labs/demo-structure with cd so that git root is demo-structure. Or set BASE_SHA/HEAD_SHA and ANCHR_REPORT_PATH to absolute path to docs/demo-artifacts/baseline.json.)

ANCHR version or commit hash:

Node version:

Timestamp:

Target path used:
