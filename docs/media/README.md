# Media assets for launch

**Demo GIF (recommended):** `demo-merge-blocked.gif`

Record 10–15 seconds:

1. Open clean PR in anchr-demo-monorepo → ArcSight check = VERIFIED.
2. Add internal import (e.g. `@market-os/core/internal`) → ArcSight = BLOCKED.
3. Attempt merge → blocked by required check.

Crop tightly. No scrolling. Use for README and DevHunt.
