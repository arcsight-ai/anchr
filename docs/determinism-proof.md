# Determinism proof (Day 1)

**PR:** sindresorhus/ky#796  
**Base:** d8d6cfe  
**Head:** 33967b4

## Run outputs

| Run | decision | violation_count | explanationHash (sha256) | runId |
|-----|----------|-----------------|--------------------------|------|
| 1   | allow | 0 | c699640d5b8d5a4eb854898b8655cafc557720dea57ec1d030ff05d209441ff7 | bea995931b24 |
| 2   | allow | 0 | c699640d5b8d5a4eb854898b8655cafc557720dea57ec1d030ff05d209441ff7 | bea995931b24 |
| 3   | allow | 0 | c699640d5b8d5a4eb854898b8655cafc557720dea57ec1d030ff05d209441ff7 | bea995931b24 |

## minimalCut (run 1)

```
[]
```

## Comparison

- minimalCut identical: true
- decision identical: true
- violation_count identical: true
- explanation hash identical: true

## Result

**Zero variance:** YES



## CI

Build fails if variance detected: `.github/workflows/determinism.yml` runs `npx tsx scripts/hardening/day1-determinism.ts` on push/PR to main.

---

## 3-PR proof (recommended for launch)

Launch blueprint recommends zero variance on **3 PRs**. This document currently proves **1 PR** (ky#796).

**To complete the 3-PR proof:**

1. Pick two more PRs (e.g. different repos or same repo, different PRs; ideally one small, one larger).
2. For each: get `base_sha` and `head_sha`, run ANCHR 3 times (e.g. via `scripts/hardening/day1-determinism.ts` with a different `PR` config, or run `npx @arcsight-ai/anchr@1 audit` manually with `GITHUB_BASE_SHA` / `GITHUB_HEAD_SHA` set).
3. Confirm same decision and same `explanationHash` across all 3 runs per PR.
4. Add a section below (or append to this file) with PR identifier, SHAs, and "Zero variance: YES".

No script change required: you can run the same comparison manually for the other two PRs, or extend `day1-determinism.ts` with two more `PR` configs and a loop.

---
*Generated by scripts/hardening/day1-determinism.ts*
